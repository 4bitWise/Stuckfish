#include "../../Libraries/include/Page.hpp"

/*namespace Stuckfish
{
	Page::Page(Core& app, Logic& logic) : _app(app), _logic(logic)
	{}

	Page& Page::GetInstance(Core& app, Logic& logic)
	{
		// The page variable is initialized once with a move assignment of a page
		// object build with the private constructor.
		static Page&& page = Page(app, logic);
		return (page);
	}
}*/